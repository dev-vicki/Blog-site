const people = ['saran', 'joe', 'alex', 'mario'];
const ages = [20, 23, 35, 26];

// console.log(people);

// module.exports = 'people';
// to access the files or data from other dest we are using export method in node.

module.exports = {
  people, ages
}